package config

const Env = "env"
const Debug = "debug"

const TokenLifetime = "token-lifetime"
const Port = "port"

const AllowedPasswords = "allowed-passwords"

const GormDialect = "gorm-dialect"

const GormConnectionString = "gorm-connection-string"

const PagesTableName = "page_data"
const ArticleTableName = "article_data"
const IconTableName = "icon_data"